//acessando uma propriedade -> 
// Propriedade são informações que podem ser verificadas de um valor
// No javascript apenas o Null e o andefined não possuem propriedades

let nome = "João";
console.log(nome.length);

let numeros = [1, 33, 3, 35, 25, 62, 7];

console.log(numeros.length);
console.log(numeros['length']);
console.log(numeros[1]);

